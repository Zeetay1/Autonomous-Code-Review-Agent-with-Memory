# CPU-only torch build: avoids pulling ~6GB of unused CUDA runtime libs that the
# default PyPI linux wheel bundles (this app only ever does CPU embedding inference).
--extra-index-url https://download.pytorch.org/whl/cpu

chromadb==1.5.2
torch==2.10.0+cpu
sentence-transformers==5.2.3
pydantic==2.12.5
langgraph==1.0.10
anthropic==0.84.0
groq==1.7.0
PyGithub==2.8.1
fastapi==0.135.1
uvicorn==0.41.0
httpx==0.28.1
pytest==9.0.2
pytest-mock==3.15.1
